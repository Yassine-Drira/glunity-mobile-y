PORT=5001
NODE_ENV=development
APP_ORIGIN=http://localhost:8081,http://localhost:19006
MONGO_URI=mongodb+srv://yassinedrira3_db_user:FUweqk0OZyS3rLao@glutenmobile.vxtr1qm.mongodb.net/glunity?appName=GlutenMOBILE
JWT_SECRET=glunity_access_super_secret_2025
REFRESH_SECRET=glunity_refresh_super_secret_2025
ACCESS_TOKEN_EXPIRES=15m
REFRESH_TOKEN_EXPIRES=7d
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=rayenmestiri08@gmail.com
SMTP_PASS=twcc tnzs eagf zqij
MAIL_FROM="GlUnity <no-reply@glunity.app>"
SOCKET_CORS_ORIGINS=http://localhost:8081,http://localhost:19006,http://localhost:3000,http://localhost:5000,http://localhost:5001
REDIS_URL=redis://localhost:6379
DISABLE_REDIS=true
PRESENCE_HEARTBEAT_INTERVAL=30000
PRESENCE_TIMEOUT=90000

# ── Cloudinary (copy values from api/.env — same account) ─────────────────────
# NOTE: These MUST be configured, otherwise uploads will fall back to local storage
# which cannot be viewed by other developers over the internet.
CLOUDINARY_CLOUD_NAME=dmdhy6rj8
CLOUDINARY_API_KEY=398584552674536
CLOUDINARY_API_SECRET=nEeIiIcbaQQbjZe8wg8_QAo5KrQ

# ── Media size limits ─────────────────────────────────────────────────────────
MAX_IMAGE_SIZE=10485760
MAX_VIDEO_SIZE=52428800

# ── Local uploads base URL (used when Cloudinary keys are not configured) ──────
APP_URL=http://localhost:5001
